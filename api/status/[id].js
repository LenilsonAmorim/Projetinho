export default async function handler(req,res){const key=process.env.PIXAZO_API_KEY;if(!key)return res.status(500).json({error:'PIXAZO_API_KEY não configurada no servidor.'});try{const r=await fetch(`https://gateway.pixazo.ai/v2/requests/status/${encodeURIComponent(req.query.id)}`,{headers:{'Ocp-Apim-Subscription-Key':key}});const text=await r.text();res.status(r.status).send(text)}catch(e){res.status(500).json({error:e.message})}}